
/**
 * Компонет рендерит отдельный сервис в виде ссылки по тем данным, которые в него приходят, 
 * @param {*} param0 
 * @returns 
 */
export  function ServiceItem({service}) {
  return (
    <a href="">{service}</a>
  )
}
